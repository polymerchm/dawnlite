onChange={() => {
                                    if (modify && isDirty) {
                                        setModify(false)
                                        setWorkingAlarm({...props.alarmListItem})
                                    } else {
                                        setModify(true)
                                    }
                                }
                                }
